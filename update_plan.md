# เปลี่ยนระบบ Onboarding Survey เป็นแบบสอบถามหลายข้อในหน้าเดียว (Multi-question Survey)

## ปัญหาปัจจุบัน
จาก Feedback ของผู้ใช้ การที่ AI ถามคำถามทีละข้อแบบ Sequential ทำให้ผู้ใช้รู้สึกว่าต้องรอนานเกินไป (รอ AI คิดคำถาม -> ตอบ -> รอ AI คิดคำถามถัดไป) 

## แนวทางการแก้ไข (Proposed Changes)
เพื่อลดระยะเวลาและทำให้ Onboarding จบในรอบเดียว เราจะเปลี่ยนจาก "การถามทีละข้อและรอคำตอบ" เป็น **"การสร้างแบบสอบถาม 3-4 ข้อรวดเดียว"** ให้ผู้ใช้ตอบในหน้าต่างเดียวแล้วกดส่งทีเดียวเลยครับ

### 1. ปรับแก้ System Instruction (agent.py)
* แก้ไขโครงสร้าง JSON ของ `survey` จากเดิมที่มีแค่ 1 `question` ให้กลายเป็น `questions` (Array) ที่สามารถใส่คำถามได้หลายข้อพร้อมกัน
* สั่งให้ AI รวบยอดคำถามในการทำความรู้จักผู้ใช้ (เช่น อาชีพ, เป้าหมาย, ปัญหาหลัก) มาเป็นแบบสอบถาม 1 ชุด (3-4 ข้อ) ในการตอบครั้งแรกครั้งเดียว เพื่อไม่ให้ผู้ใช้ต้องรอนาน

### 2. ปรับแก้ Frontend (app.js & style.css & index.html)
* ปรับปรุงโค้ด `openMcqModal` ให้สามารถ Loop สร้างคำถามหลายๆ ข้อใน Modal เดียวได้
* เปลี่ยนรูปแบบ UI จากปุ่มกดตัวเลือกข้อเดียว มาเป็นลักษณะ **Radio Buttons หรือ Button Group แยกตามแต่ละคำถาม**
* กรณีที่บางคำถามผู้ใช้เลือก "อื่นๆ (โปรดระบุ)" กล่อง Text Input จะโผล่ขึ้นมาเฉพาะใต้คำถามข้อนั้นๆ อย่างเป็นระเบียบ
* เมื่อกด "ส่งคำตอบ" ระบบจะรวบรวมคำตอบทุกข้อเข้าด้วยกัน เช่น `[ตอบแบบสอบถาม] Q1: พนักงานประจำ | Q2: หมดไฟในการทำงาน | Q3: ต้องการ Work-life balance` แล้วส่งไปให้ AI ทีเดียว

## User Review Required
> [!IMPORTANT]  
> การเปลี่ยนเป็นแบบสอบถามยาวทีเดียว จะทำให้ Branching Logic (เช่น ถ้าตอบว่าเป็นพนักงาน ค่อยถามเรื่องย้ายงาน) หายไปบางส่วน เพราะ AI จะสร้างคำถามทั้งหมด 3-4 ข้อมาพร้อมกันตั้งแต่แรก แต่ข้อดีคือ **เร็วและจบในหน้าเดียว** ไม่ทราบว่าคุณผู้ใช้เห็นด้วยกับแนวทาง "รวบยอดคำถามในหน้าเดียว" นี้ไหมครับ? หรืออยากให้มีวิธีแก้แบบอื่น?
